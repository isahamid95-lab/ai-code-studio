{
  "hooks": {
    "beforeFileEdit": [
      {
        "command": "npm run lint",
        "condition": "file.endsWith('.ts') || file.endsWith('.tsx')"
      }
    ],
    "afterFileEdit": [
      {
        "command": "echo 'File saved - run tests with npm test'"
      }
    ]
  }
}
